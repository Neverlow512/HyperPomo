tkcalendar>=1.6.1
playsound>=1.3.0
pyinstaller>=6.0.0 
# pygobject>=3.40.0 # Recommended for playsound on Linux, best installed via system package manager (e.g., apt install python3-gi)
google-generativeai>=0.5.0
pyttsx3>=2.90
SpeechRecognition>=3.10.0
PyAudio>=0.2.11 # Note: May require manual installation steps on some systems (e.g., sudo apt-get install python3-pyaudio)
yt-dlp>=2023.12.30
python-vlc>=3.0.20 # Note: Requires VLC media player to be installed separately on the system.
